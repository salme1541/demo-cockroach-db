server:
  port: 8080

spring:
  profiles:
    active: local
---

spring:
  profiles: local
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL94Dialect
    show-sql: true
  datasource:
    url: jdbc:postgresql://dave.nexr.com:26257/examdb?sslmode=disable
    username: dave
    password:

server:
  port: 8081

---
server:
  port: 8082
spring:
  profiles: dev